<?xml version="1.0"?>
<launch>
  <!-- load world -->
  <node name="gazebo" pkg="gazebo_ros" type="gazebo" args="$(find r2_gazebo)/gazebo/r2_empty_no_gravity.world" output="screen" respawn="false" />

  <!-- load controller parameters-->
  <rosparam file="$(find r2_gazebo)/config/controllers/r2sim_torso_params.yaml" command="load" />

  <!-- load robot -->
  <param name="/use_sim_time" value="true"/>
  <param name="robot_description" command="$(find xacro)/xacro '$(find r2_description)/robots/r2_orocos.sim.urdf.xacro'" />
  <node name="spawn_r2_orocos" pkg="gazebo_ros" type="spawn_model" args="-urdf -param robot_description -model r2_orocos" respawn="false" output="screen" />
  <node name="robot_state_publisher" pkg="robot_state_publisher" type="robot_state_publisher" />

  <!-- start gui -->
  <node name="gazebo_gui" pkg="gazebo_ros" type="gzclient" respawn="false" output="screen" />
  
</launch>

